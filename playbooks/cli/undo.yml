- name: Undo bashrc
  hosts: localhost
  connection: local
  tasks:
    - name: Remove ubctl function from {{ ansible_env.HOME }}/.bashrc
      lineinfile:
        path: "{{ ansible_env.HOME }}/.bashrc"
        regexp: '^ubctl\(\) \{.*\}$'
        state: absent

